#include<stdio.h>
#include<ctype.h>

int main(){

//  c= getchar();
char d;
//  if ((d=getchar())!=EOF && (isxdigit(d)))
if ((d=getchar())!=EOF)
//  if (isxdigit(d))
 {
    printf("string to hex: %c",d);
 }
// else

// char e;
// e= (isxdigit(int d));
// printf("%c", e);

// // for(d=10;d>='A' && d <= 'F';d++){
// //     printf("string:%c",d);
// // }

}